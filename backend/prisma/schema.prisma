generator client {
  provider = "prisma-client-js"
  output = "../src/prisma-client"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Users {
  id String @id @unique
  name String
  email String @unique
  password String
  phoneNumber String
  iban String
  foto String?

  Addresses Addresses[]
  Condominiums Condominiums[]
  AreaReservations AreaReservations[]
  Services Services[]
  ServiceRequests ServiceRequests[]
  Orders Orders[]
  Votings Votings[]
}

model Condominiums {
  id String @id @unique
  name String
  email String @unique
  phoneNumber String
  monthlyQuota Float
  adminId String
  createdAt DateTime @default(now())
  admin Users @relation(fields: [adminId], references: [id], onDelete: Cascade)

  Addresses Addresses[]
  CommonAreas CommonAreas[]
  Services Services[]
  Orders Orders[]
}

model Addresses {
  id String @id @unique
  country String
  city String
  street String
  houseNumber String
  postalCode String
  houseType Int
  createdAT DateTime @default(now())
  userId String
  condominiumId String
  user Users @relation(fields: [userId], references: [id], onDelete: Cascade)
  condominium Condominiums @relation(fields: [condominiumId], references: [id], onDelete: Cascade)

  CondominiumPayments CondominiumPayments[]
}

model CommonAreas {
  id String @id @unique
  name String
  startSchedule String
  endSchedule String
  cost Float?
  rules String
  status String
  capacity Int
  images String[]
  type Int
  createdAT DateTime @default(now())
  condominiumId String
  condominium Condominiums @relation(fields: [condominiumId], references: [id], onDelete: Cascade)

  AreaReservations AreaReservations[]
}

model AreaReservations {
  id String @id @unique
  startDate DateTime
  endDate DateTime
  status String
  areaId String
  userId String
  area CommonAreas @relation(fields: [areaId], references: [id], onDelete: Cascade)
  user Users @relation(fields: [userId], references: [id], onDelete: Cascade)

  CondominiumPayments CondominiumPayments[]
  Fine Fine[]
}

model CondominiumPayments {
  id String @id @unique
  value Float
  date DateTime
  paymentType Int
  addressId String
  areaReservationId String?
  address Addresses @relation(fields: [addressId], references: [id], onDelete: Cascade)
  areaReservation AreaReservations? @relation(fields: [areaReservationId], references: [id], onDelete: Cascade)
}

model Fine {
  id String @id @unique
  amount Float
  reason String
  createdAt DateTime
  status String
  areaReservationId String
  areaReservation AreaReservations @relation(fields: [areaReservationId], references: [id], onDelete: Cascade)
}

model Services {
  id String @id @unique
  name String
  description String
  cost Float?
  ownerId String
  condominiumId String
  createdAt DateTime @default(now())
  owner Users @relation(fields: [ownerId], references: [id], onDelete: Cascade)
  condominium Condominiums @relation(fields: [condominiumId], references: [id], onDelete: Cascade)

  ServiceRequests ServiceRequests[]
}

model ServiceRequests {
  id String @id @unique
  requestDate DateTime @default(now())
  status String
  serviceId String
  userId String
  service Services @relation(fields: [serviceId], references: [id], onDelete: Cascade)
  user Users @relation(fields: [userId], references: [id], onDelete: Cascade)

  ServiceReviews ServiceReviews[]
}

model ServiceReviews {
  id String @id @unique
  rating Int
  comment String
  createdAt DateTime  @default(now())
  serviceRequestId String
  serviceRequest ServiceRequests @relation(fields: [serviceRequestId], references: [id], onDelete: Cascade)
}

model Orders {
  id String @id @unique
  description String
  createdAt DateTime @default(now())
  urgency String
  status String
  lastOrder String?
  startDate DateTime?
  endDate DateTime?
  votingDeadline DateTime?
  userId String
  condominiumId String
  user Users @relation(fields: [userId], references: [id], onDelete: Cascade)
  condominium Condominiums @relation(fields: [condominiumId], references: [id], onDelete: Cascade)

  Budgets Budgets[]
  Votings Votings[]
  OrderWorks OrderWorks[]
}

model Budgets {
  id String @id @unique
  description String
  amount Float
  createdAt DateTime @default(now())
  orderId String
  order Orders @relation(fields: [orderId], references: [id], onDelete: Cascade)

  Votings Votings[]
}

model Votings {
  userID String 
  orderID String 
  decision Boolean
  budgetID String?
  order Orders @relation(fields: [orderID], references: [id], onDelete: Cascade)
  budget Budgets? @relation(fields: [budgetID], references: [id], onDelete: Cascade)
  user Users @relation(fields: [userID], references: [id], onDelete: Cascade)

  @@id([userID,orderID])
}

model OrderWorks {
  id String @id @unique
  postedAt DateTime
  status String
  description String
  orderId String
  order Orders @relation(fields: [orderId], references: [id], onDelete: Cascade)
}